# OS/editor cruft not already covered by each project's own .gitignore
.DS_Store
Thumbs.db
.vscode/*
!.vscode/extensions.json
.idea

# Local DB dumps for backup/restore convenience — not committed by Claude; user uploads manually.
/db-backup/
